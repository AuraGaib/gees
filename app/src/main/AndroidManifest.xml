<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geesprod.gees">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        xmlns:tools="http://schemas.android.com/tools"
        tools:replace="android:theme"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity_login"
            android:screenOrientation="portrait" />
        <activity android:name=".activity_daftar" />
        <activity android:name=".activity_dashboard" />
        <activity android:name=".activity_admin" />
        <activity android:name=".activity_admin_katagori" />
        <activity android:name=".activity_tambah_dagangan" />
        <activity
            android:name=".activity_home_user"
            android:label="@string/title_activity_home_user"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity_setting" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".activity_detail_produk"></activity>
    </application>

</manifest>